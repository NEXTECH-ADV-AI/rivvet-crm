/** @deprecated Prefer `@/lib/crm/store` + `@/lib/crm/seed`. Kept for import stability. */
export {
  DEMO_NOW,
  seedLeads as leads,
  seedAccounts as accounts,
  seedOpportunities as opportunities,
  seedActivities as activities,
  seedContacts as contacts,
  seedStageEvents as stageEvents,
  seedSendDocuments as sendDocuments,
  seedPayments as payments,
  users,
  ago,
  inDays,
} from "./seed";
